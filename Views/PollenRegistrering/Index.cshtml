@model IEnumerable<Eksamen2025Gruppe5.Models.PollenRegistrering>
@{
    ViewData["Title"] = "Pollenregistreringer";
}

<h1>Pollenregistreringer</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Ny registrering</a>
</p>
@if (!Model.Any())
{
    <div class="alert alert-info">Det finnes ingen pollenregistreringer.</div>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Dato</th>
                <th>Område</th>
                <th>Nivå</th>
                <th>Handlinger</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>@item.Dato.ToShortDateString()</td>
                <td>@item.Område</td>
                <td>@item.Nivå</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary btn-sm">Rediger</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Slett</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
